(ns aoc.core-test
  (:require [aoc.core :as sut]
            [clojure.test :refer :all]))

(deftest basic-tests
  (testing ""
    (is (= 1 1))))
;; (deftest digits-tests
;;   (is (= (digits 10) '(1 0)))
;;   (is (= (digits 1234) '(1 2 3 4))))
